name = Sign
description = A sign whose contents can be read by a leader.

custom_category_name = Misc
height = 16
prompt_text = Read
pushable = false
pushes = false
radius = 16
trigger_range = 64

area_editor_properties {
    Text {
        var = text
        type = text
        tooltip = Text to show when the player reads the sign. Use \\n to write a new line.
    }
}
